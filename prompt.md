## AIQA
### 写一个网页导航页面，放在docs/index.html中
```
写一个网页导航页面，放在docs/index.html中，要求如下：
1. 该网页在浏览器中打开时，显示一个导航栏，导航栏中包含多个按钮，每个按钮对应一个页面。
2. 每个按钮点击时，跳转到对应的页面。
3. 每个页面都包含一个标题，一个内容区域，一个导航栏。
导航地址包括但不限于：
1. https://tgnav.github.io/
```

### 请将导航改为二级导航
```
请将导航改为二级导航，要求如下：
1. 把导航数据存入到josnd对象中，并使用json.loads()方法加载,以便添加数据时，只需要添加数据，不需要修改代码。
2. json对象中包含多个一级导航，每个一级导航包含多个二级导航
3. 一级导航点击时，显示对应的二级导航
4. 二级导航点击时，跳转到对应的页面
```